name: face_recognition_system
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  image_picker:
  image:
  camera:
  google_mlkit_face_detection:
  google_mlkit_commons:
  tflite_flutter:
  sqflite:
  path_provider:
  path: any
#  google_fonts: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/tflite_models/mobile_face_net.tflite

